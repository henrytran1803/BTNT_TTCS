CREATE VIEW view_TPNT AS

SELECT TP.MATPNT, TP.TENTP, TG.HO, TG.TEN, TP.NAM, QT.TENQT, CD.TENCHUDE, TD.TENTD, TP.DIENGIAI

FROM TPNT TP

JOIN TAC_GIA TG ON TG.MATACGIA = TG.MATACGIA

JOIN CHUDE CD ON CD.MACD = TP.MACD

JOIN THOI_DAI TD ON TP.MATD = TD.MATD

JOIN QUOC_GIA QT ON QT.MAQT = TP.MAQT;